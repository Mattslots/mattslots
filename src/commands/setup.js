const { SlashCommandBuilder, PermissionFlagsBits, MessageFlags } = require("discord.js"); const { brandEmbed } = require("../utils/embed");
module.exports={data:new SlashCommandBuilder().setName("setup").setDescription("Affiche l’ordre d’installation de MattSlots V5.1").setDefaultMemberPermissions(PermissionFlagsBits.Administrator),async execute(i){await i.reply({embeds:[brandEmbed("⚙️ Installation MattSlots V5.1","Exécute dans cet ordre :\n\n1️⃣ `/verification`\n2️⃣ `/roles`\n3️⃣ `/tickets`\n4️⃣ `/permissions`\n\nPuis configure les salons avec `/config` et teste `/status`.\n\n⚠️ Place le rôle du bot au-dessus des rôles qu’il doit attribuer.")],flags:MessageFlags.Ephemeral});}};
